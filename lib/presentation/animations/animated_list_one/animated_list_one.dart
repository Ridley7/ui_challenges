import 'package:flutter/material.dart';

class AnimatedListOne extends StatelessWidget {
  const AnimatedListOne({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
